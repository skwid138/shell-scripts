# Local, machine-specific overrides (sourced by vars.sh / vars.local.zsh, never committed)
vars.local.sh
vars.local.zsh
*.local.sh
*.local.zsh

# Snapshots / backups
*.backup-*
.backup-*

# OS / editor cruft
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Test artifacts
# Bats test helpers are tracked as git submodules (see .gitmodules).
# Do not gitignore tests/test_helper/* — keep submodule pointers committed.
# (Kept as git submodules — actual tracking happens via .gitmodules; this entry
#  is a safety net in case they're ever cloned without --recurse-submodules.)

# Working docs not for publication (private restructure plans, etc.)
.project-plans
KEYMAPS-CHEATSHEET.md
